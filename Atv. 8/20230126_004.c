#include <stdio.h>

int main(){
    int matriz[3][3], x;
    for(int i=0; i<3; i++){
        for(int j=0; j<3; j++){
            printf("Digite os numeros da matriz: ");
            scanf("%d", &matriz[i][j]);
        }
    }
    printf("Digite um valor qualquer: ");
    scanf("%d", &x);
    for()
}
